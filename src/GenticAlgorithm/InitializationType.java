package GenticAlgorithm;

//ENUM that checks which kind of initializing algorithm was choosen by user.
public enum InitializationType {
    HILLCLIMBING,
    SIMULATEDANNEALING,
    RANDOM;
    private InitializationType() {
    }
}
